# PostgreSQL connection string injected by runtime and docker compose.
DATABASE_URL=postgres://app:app_secret@localhost:5432/app?sslmode=disable

# HTTP listen port preferred by container runtime.
PORT=8080

# Optional fallback HTTP listen port used when PORT is unset.
APP_PORT=8080
