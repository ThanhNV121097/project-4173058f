# Browser-visible backend origin used by client-side code.
NEXT_PUBLIC_API_URL=http://localhost:8080

# Server-side backend origin used by Next.js server rendering and route handlers.
API_ORIGIN=http://localhost:8080
